# 语言预习工作流 · 计划书（书籍版）

> **状态: P0 已完成**(2026-08-29)——《Little Women》47 章、696 张卡片、生词总库 704 词、标注版、推荐报告全部交付。详见 README.md。

> 参考视频《skill+美剧 学英语也太高效了》的方法论，把"追剧学英语"流程迁移到"读原著学英语"。
> 核心理念（视频原话）：**不要背单词，要识别单词** —— 先提取生词预习、建立短期记忆，再在阅读中识别。

## 0. 目标与优先级（2026-08-29 用户确认）

1. **书籍模式 P0 必交付**：英语原著 EPUB → 按章生词卡片（本轮跑完必须能用）
2. **语言扩展**：架构支持多语言，日语第二优先（语言适配层设计见 §7）
3. **美剧模式**：延后实现，暂不投入；后期复用 wenyi 的 SRT 字幕管线
4. **参考 wenyi 项目**（BigDawnGhost/wenyi，MIT）：借鉴设计 + 增强输出（见 §9）

---

## 1. 视频方法论 → 书籍版对应

| 视频（美剧模式） | 书籍模式（本项目） |
|---|---|
| 输入剧名+集数 | 输入一本 EPUB 原著 + 章节范围 |
| 找该集字幕 | EPUB 转 Markdown，按章切分 |
| 筛 50 个 B2–C1 词 | 同标准筛选，数量可调（默认 50/本） |
| 配音标、例句 | 音标 + 本书原文例句（真实语境）+ 中文译文 |
| 导出记忆卡片工具格式 | 导出 Anki 可导入的卡片（TSV / .apkg） |
| 刷 15 分钟卡片 → 建立短期记忆 | 同样刷卡（间隔重复） |
| 看剧时识别单词 | 读书时识别单词（原句就是卡片例句的来源） |
| （视频无此步） | **维护跨书累积的"生词总库" + 按价值推荐生词** |

---

## 2. 端到端工作流（一次书籍预习的完整流程）

```
用户发来 EPUB
  ↓ ① 转换       EPUB → Markdown（内置 to_markdown 工具，原生支持 .epub）
  ↓ ② 清洗切分   去版权页/目录噪音，按章节切分为学习单元，保留章节标题
  ↓ ③ 词形还原   分词 + lemmanize（simplemma 库），得到 lemma 级别词条
  ↓ ④ 候选过滤   排除：功能词/停用词、专有名词（人名地名）、数字、A1–A2 基础词、
  │              用户"已认识词表"中的词、已推荐过的词
  ↓ ⑤ 级别判定   CEFR 标注（A1–C1+）：
  │              · EFLLex 词表（A1–C1 分级 + 各级频率）
  │              · Oxford 3000/5000（≤B1 基线 / B2–C1 目标）
  │              · 未命中 → 标记 C2/学术/超纲，走在线兜底查询
  ↓ ⑥ 生僻度打分  通用词频（wordfreq zipf 值）× 书中出现次数 × CEFR 价值 → 加权排序
  ↓ ⑦ 选词       分章选词：每章 15–20 个，以 B2–C1 为主，C2/学术词少量点缀
  ↓ ⑧ 释义+例句  中文释义（模型生成 + 本地词典校验），例句从本书原文抽取含该词的句子，
  │              并配中文译文；音标取本地 ECDICT，未命中走 dictionaryapi.dev 在线兜底
  ↓ ⑨ 生成卡片   输出 Anki 可导入文件（TSV 或 .apkg）
  ↓ ⑩ 更新总库   生词总库 master_wordlist.csv 去重合并，记录来源书/章节/次数/状态
  ↓ ⑪ 推荐报告   从总库中按"高频高价值 & 未掌握"排序，输出推荐清单（可含周推荐）
  ↓ ⑫ 标注版     生词标注版章节文本（把卡片中的词在原文高亮，供读书时"识别"）
```

**用户侧闭环**：导入 Anki → 每天刷 15 分钟 → 读原书时识别 → 认识后标记 → 总库状态流转为 known → 后续不再重复推荐。

---

## 3. 目录结构

```
LanguagePreviewAgentFlow/
├── PLAN.md                     ← 本计划
├── data/
│   ├── books/                  ← 【输入】用户放置 EPUB 的位置
│   ├── books/_md/              ← 转换后的 Markdown 缓存
│   └── output/                 ← 【输出】每本书的 Anki 卡片 + 推荐报告
├── vocabulary/
│   ├── master_wordlist.csv     ← 核心资产：跨书累积的生词总库
│   ├── known_words.txt         ← 用户已掌握词（手动标记后累积）
│   └── pending.txt             ← 已推荐待确认词（刷卡完成自动归档）
├── resources/                  ← 词表库（一次性下载，之后离线可用）
│   ├── efllex_cefr.csv         ← CEFR 分级词表（A1–C1）
│   ├── oxford3000.txt / oxford5000.txt
│   ├── ecdict.mini.csv         ← 本地词典（音标/词频/释义，离线）
│   └── stopwords_en.txt
└── scripts/
    ├── config.py               ← 参数：语言、水平基线、每章推荐数、级别偏好…
    ├── pipeline.py             ← 主流程（①–⑫ 串联，分章断点续跑，state/<book>/）
    ├── language_adapters/      ← 语言适配层（§7）
    │   ├── en_adapter.py       ← 英语：simplemma + EFLLex/Oxford + ECDICT
    │   └── ja_adapter.py       ← 日语：fugashi/MeCab + JLPT 词表 + jMdict（P2）
    ├── cefr.py                 ← 级别判定（本地词表 + 兜底）
    ├── cards.py                ← 卡片组装：TSV 导出
    ├── wordlist.py             ← 生词总库读写/合并/状态流转
    └── annotate.py             ← 增强：生词标注版章节文本
tools/
└── wenyi/                      ← BigDawnGhost/wenyi（MIT，clone 参考：EPUB 解析/双语/SRT）
```

运行方式：`uv run --with simplemma --with wordfreq --with genanki python scripts/pipeline.py <book.epub>`
（环境：Cherry 内置 uv；脚本可跨 Windows/macOS 运行）

---

## 4. 生词总库 schema（master_wordlist.csv）

```
word, pos, cefr, zipf, first_seen, status,
sources,               # 来源清单：book|chapter|freq 组合，如 "The_Hobbit|ch3|2; ..."
freq_total,            # 跨书累积出现次数
example_en, example_cn,# 最新一条原文例句 + 译文
recommended_date,      # 最后一次被推荐的日期
card_exported          # 是否已导出过卡片（已导出 = 短期内不重复推荐）
```

**状态流转**：`pending`（待学）→ `active`（已导卡片，刷词中）→ `known`（已掌握）→ `archived`（长期未掌握降权）。总库自动去重合并：同词不同书只留一行，累积频次与来源。

---

## 5. Anki 卡片设计

**笔记类型字段**（Note Type）：`单词 | 音标 | 词性 | 中文释义 | CEFR级别 | 原文例句 | 例句译文 | 来源`

- 卡片正面：单词 + 音标 + 词性（可选：点击发音）
- 卡片背面：中文释义 + CEFR 徽章 + **原文例句**（目标词加粗）+ 例句译文 + 来源（书名·章节）
- 记忆锚点理念：例句全部来自书里原文 → 读书时"识别"的正是卡片上见过的句子，与视频"看剧识别台词"完全同构

**导出格式**：
- 默认：**TSV**（UTF-8，首行字段名）→ Anki 桌面端 File → Import 直接导入，字段自动映射
- 可选：**.apkg**（genanki 打包，双击即装，含笔记模板与样式）→ `scripts/make_anki_template.py` 生成 `resources/anki/anki_template.apkg`

---

## 6. 推荐策略（从总库挑"值得现在学"的词）

```
推荐分 = 0.4 × CEFR 价值(B2=1.0, C1=1.2, C2=0.8, A1–A2=0)
       + 0.3 × 书中出现次数归一化         ← 书中反复出现 = 首学回报最高
       + 0.2 × 通用词频(zipf 越接近 4–6 越值)  ← 太生僻(专有/冷僻)反而降权
       + 0.1 × 词性价值(动/形容 > 名词)
− 排除：known、已导出未遗忘期内的、A1–A2、停用词、专有名词
```

- 每本书交付：**按章卡片集**（每章 15–20 词，卡片 TSV 按章拆分，方便边读边推）+ **推荐报告**（分档：必背 B2–C1 / 提高 C1–C2 / 拓展学术词）
- 可选扩展：`cron` 每周推送"本周 20 词"（从总库取未推荐的最高分词汇）

---

## 7. 多语言支持设计（语言适配层）

管线主体（清洗→候选过滤→选词→卡片→总库）对语言无关；把"语言差异"收敛到一个 adapter 接口：

```
class LanguageAdapter:
    tokenize(text) -> list[Token]      # 切词 / 分词
    lemmatize(token) -> str            # 词形还原（日语 ≈ 词典形）
    lookup(word) -> WordInfo           # 级别 / 注音或音标 / 词义 / 词频
    is_proper_noun(token) -> bool      # 专有名词排除
    is_stopword(token) -> bool
```

| 能力 | 英语 adapter（P0） | 日语 adapter（P2） |
|---|---|---|
| 分词/词形还原 | simplemma | fugashi(MeCab) + unidic-lite（输出词典形 & 读音） |
| 级别判定 | EFLLex A1–C1 + Oxford 3000/5000 | JLPT 分级（N5–N1）+ CEFR-J 词表（A1–B2） |
| 注音/音标 | ECDICT（离线）→ dictionaryapi.dev 兜底 | MeCab 自带读音 → 卡片用假名注音 |
| 词义 | ECDICT 中文释义 | jMdict（离线） |
| 专名排除 | 启发式（首字母大写+低词频） | MeCab 固有名词分类（人名/地名/组织） |

日语卡片字段等价替换：`音标 → 假名注音`、`CEFR → JLPT 等级`；TSV 结构与 Anki 导入方式不变。
**新语言接入 = 新写一个 adapter + 一份词表资源，管线核心零改动。**

---

## 8. 词表资源与兜底（已完成调研，链接实存）

| 用途 | 主方案 | 备选 |
|---|---|---|
| CEFR 级别 | EFLLex（UCLouvain，A1–C1 分级 CSV，免费教育使用；已确认可下载） | Oxford 3000/5000（GitHub CSV，已确认） |
| 音标/词频/释义 | 本地 ECDICT.mini（离线 CSV，含音标、BNC 词频、中文释义） | dictionaryapi.dev（免费在线 API） |
| 文中词频 | wordfreq（Python 库，zipf 词频） | — |
| 词形还原 | simplemma（轻量纯 Python） | spacy（较重） |

---

## 9. 结合 wenyi 项目（BigDawnGhost/wenyi · MIT）

**调研结论**：wenyi 是"一条命令把 EPUB 整本翻译成可读中文版"的工具（全书预扫描、实时术语表、断点续跑、双语 EPUB、SRT 字幕路径），Python/uv 技术栈。
**定位**：它"翻译整本书给人读"，本项目"提取生词做预习卡片"，目标互补。不把管线做成依赖 wenyi，而是**借鉴其设计 + 复用其特定模块 + 借鉴双语能力做增强输出**。

### 借鉴的设计（写进我们自己的管线）
1. **实时术语表（glossary）机制 → 强化生词总库**：wenyi 随翻译推进实时提取专名/术语/反复表达并做冲突检测；我们的总库同样"随书累积、跨章去重"，其冲突检测思路用于一词多义处理（本书语境义优先，卡片标注语境义）。
2. **状态目录 + 断点续跑**：`state/<book>/` 记录每章处理状态，中断后同命令续跑 —— 匹配"分章推荐"的节奏。
3. **专名提取/语言自检**：其自动语言检测逻辑可复用于多语言输入识别。

### 增强功能（本项目新增，把"读书识别"闭环做实）
- **生词标注版书籍（annotated）**：每章处理完，输出该章高亮版文本/HTML（卡片里的 15–20 词在原文加粗高亮）——刷完卡翻开书第一眼"识别"的就是背过的词，与视频"看剧识别台词"完全同构（已入流程图 ⑫）。
- **双语对照阅读版（可选）**：借鉴 wenyi 双语 EPUB 思路：原文 + 弱化中文对照，例句所在句自动对齐。
- **SRT 字幕管线（美剧模式，延后）**：wenyi 已实现 SRT 解析与双语字幕输出，未来做美剧模式时直接复用其 SRT 路径。

### 落地方式
`git clone` 到 `tools/wenyi/`（MIT），P0 实施时随管线一起引入；仅定向复用其 EPUB 解析/对齐代码与 SRT 路径，**不引入其翻译主流程**。核心管线保持自研、轻量、语言无关。

---

## 10. 实施阶段

- **P0 英语书籍管线（本轮必交付）**：clone wenyi → 资源下载（EFLLex/Oxford/ECDICT）→ 管线脚本 + en_adapter → 用 1 本 EPUB 完整走一遍 ①–⑫ → 产出按章 Anki TSV + 生词总库 + 推荐报告 + 生词标注版章节文本
- **P1 打磨**：词性/释义质量调参、例句去重（同义词形句子合并）、词典兜底查询、双语对照输出
- **P2 日语 adapter**：fugashi 分词 + JLPT/CEFR-J 词表 + jMdict + 假名注音 → 日语卡片跑通
- **P3 扩展（延后）**：美剧模式（复用 wenyi 的 SRT 管线）、每周推荐 cron、与 Anki 间隔重复数据联动

---

## 11. 已确认参数（2026-08-29 用户确认）

| 参数 | 取值 |
|---|---|
| 英语水平基线 | **考研英语二（高于四级）** → 排除 A1–A2；**B1 少量配额**（书中高频 B1 词 ≤ 每章 10%）；主推 B2–C1，C1–C2 少量 |
| 推荐数量 | **分章推荐**：每章 15–20 词，配套边读边推的学习节奏 |
| 卡片格式 | **TSV**（UTF-8，首行字段名，Anki 直接导入）；.apkg 留作可选扩展 |
| 例句形态 | **书中原句 + 中文译文**；书中无完整句时 AI 补一句贴近文风的例句 |
| 语言优先级 | **英语先行（P0 必交付），日语第二（P2）**；按语言适配层设计 |
| 美剧模式 | 延后实现，暂不投入；后期复用 wenyi 的 SRT 字幕管线 |
| wenyi 结合 | 借鉴设计（实时术语表/状态续跑）+ 增强输出（生词标注版、双语对照） |